<!DOCTYPE html>
<html>
<head>
  <title>RoomMatch</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-..." crossorigin="anonymous" referrerpolicy="no-referrer" />

</head>

<body class="flex flex-col min-h-screen bg-gray-50 text-gray-900">
<!-- Navbar -->
<nav class="bg-white shadow-md">
  <div class="container mx-auto px-6 py-4 flex justify-between items-center">
    <div class="text-2xl font-bold text-blue-600">
      RoomMatch
    </div>
    <div class="space-x-6">
      <% if user_signed_in? %>
        <%= link_to "Swipe", root_path, class: "text-gray-700 hover:text-blue-600 font-semibold" %>
        <%= link_to "Matches", matches_path, class: "text-gray-700 hover:text-blue-600 font-semibold" %>
        <%= link_to "Profile", edit_profiles_path, class: "text-gray-700 hover:text-blue-600 font-semibold" %>
        <%= link_to "Filters", edit_filters_path, class: "text-gray-700 hover:text-blue-600 font-semibold" %>
        <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "text-red-500 hover:text-red-700 font-semibold" %>
      <% else %>
        <%= link_to "Login", new_user_session_path, class: "text-gray-700 hover:text-blue-600 font-semibold" %>
        <%= link_to "Sign Up", new_user_registration_path, class: "text-gray-700 hover:text-blue-600 font-semibold" %>
      <% end %>
    </div>
  </div>
</nav>

<!-- Main content -->
<main class="flex-grow container mx-auto px-6 py-10">
  <%= yield %>
</main>

<!-- Footer -->
<footer class="bg-white border-t mt-12">
  <div class="container mx-auto px-6 py-4 text-center text-sm text-gray-600">
    &copy; <%= Time.now.year %> RoomMatch. All rights reserved.
  </div>
</footer>
</body>
</html>
