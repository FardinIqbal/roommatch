<div class="flex <%= message.sender == local_user ? 'justify-end' : 'justify-start' %>">
  <div class="<%= message.sender == local_user ? 'bg-blue-500 text-white' : 'bg-gray-200 text-gray-900' %> px-4 py-2 rounded-lg max-w-xs">
    <%= message.body.presence || '...' %>
    <p class="text-xs text-gray-500 mt-1"><%= time_ago_in_words(message.created_at) %> ago</p>
  </div>
</div>
