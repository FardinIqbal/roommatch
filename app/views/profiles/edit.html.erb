<div class="max-w-md mx-auto mt-10 p-6 bg-white shadow-lg rounded-lg">
  <h2 class="text-2xl font-bold mb-6 text-center">Edit Your Profile</h2>

  <%= form_with model: @user, url: profiles_path, method: :patch, local: true, html: { multipart: true } do |f| %>
    <div class="mb-4">
      <%= f.label :name, "Name", class: "block text-sm font-medium text-gray-700" %>
      <%= f.text_field :name, class: "mt-1 block w-full border rounded p-2" %>
    </div>

    <div class="mb-4">
      <%= f.label :bio, "Bio", class: "block text-sm font-medium text-gray-700" %>
      <%= f.text_area :bio, rows: 4, class: "mt-1 block w-full border rounded p-2" %>
    </div>

    <div class="mb-4">
      <%= f.label :school, "School", class: "block text-sm font-medium text-gray-700" %>
      <%= f.text_field :school, class: "mt-1 block w-full border rounded p-2" %>
    </div>

    <div class="mb-4">
      <%= f.label :major, "Major", class: "block text-sm font-medium text-gray-700" %>
      <%= f.text_field :major, class: "mt-1 block w-full border rounded p-2" %>
    </div>

    <div class="mb-4">
      <%= f.label :year_in_school, "Year in School", class: "block text-sm font-medium text-gray-700" %>
      <%= f.select :year_in_school, ["Freshman", "Sophomore", "Junior", "Senior", "Graduate"], {}, class: "mt-1 block w-full border rounded p-2" %>
    </div>

    <div class="mb-4">
      <%= f.label :sleep_schedule, "Sleep Schedule", class: "block text-sm font-medium text-gray-700" %>
      <%= f.select :sleep_schedule, [["Early Bird", "early"], ["Night Owl", "night"]], {}, class: "mt-1 block w-full border rounded p-2" %>
    </div>

    <div class="mb-4">
      <%= f.label :clean, "Are You a Clean Roommate?", class: "block text-sm font-medium text-gray-700" %>
      <%= f.select :clean, [["Yes", true], ["No", false]], {}, class: "mt-1 block w-full border rounded p-2" %>
    </div>

    <div class="mb-4">
      <%= f.label :hobbies, "Hobbies/Interests", class: "block text-sm font-medium text-gray-700" %>
      <%= f.text_area :hobbies, rows: 3, class: "mt-1 block w-full border rounded p-2" %>
    </div>

    <div class="mb-4">
      <%= f.label :guest_policy, "Guest Policy", class: "block text-sm font-medium text-gray-700" %>
      <%= f.select :guest_policy, ["Okay with guests", "No guests preferred"], {}, class: "mt-1 block w-full border rounded p-2" %>
    </div>

    <div class="mb-4">
      <%= f.label :smoking, "Do You Smoke?", class: "block text-sm font-medium text-gray-700" %>
      <%= f.select :smoking, [["Yes", true], ["No", false]], {}, class: "mt-1 block w-full border rounded p-2" %>
    </div>

    <div class="mb-4">
      <%= f.label :pets, "Do You Have Pets?", class: "block text-sm font-medium text-gray-700" %>
      <%= f.select :pets, [["Yes", true], ["No", false]], {}, class: "mt-1 block w-full border rounded p-2" %>
    </div>

    <div class="mb-6">
      <%= f.label :avatar, "Profile Picture", class: "block text-sm font-medium text-gray-700" %>
      <%= f.file_field :avatar, class: "mt-1 block w-full border rounded p-2" %>

      <% if @user.avatar.attached? %>
        <div class="mt-4 text-center">
          <p class="text-sm text-gray-500 mb-2">Current Picture:</p>
          <%= image_tag @user.avatar.variant(resize_to_limit: [150, 150]), class: "mx-auto rounded-full shadow" %>
        </div>
      <% end %>
    </div>

    <%= f.submit "Save Profile", class: "bg-blue-500 hover:bg-blue-600 text-white font-semibold px-4 py-2 rounded w-full" %>
  <% end %>
</div>
